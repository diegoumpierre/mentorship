# CSR - Client-Side Rendering

## How to run

Just open `index.html` in your browser. No server needed.

```bash
open index.html
```

## What to notice

- View the page source (Ctrl+U) — you'll see almost no content, just "Loading..."
- The page content is built entirely by JavaScript in the browser
- The timestamp is generated client-side
