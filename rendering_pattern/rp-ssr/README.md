# SSR - Server-Side Rendering

## How to run

```bash
npm install
npm start
```

Then open http://localhost:3001

## What to notice

- View the page source — the full content is already there, no JavaScript needed
- Refresh the page — the timestamp changes every time (server builds a fresh page per request)
- There is no "Loading..." state; content is ready from the start
